# Odoo 17 integration
# Leave empty when React is served from Odoo (same origin) or using Vite proxy
VITE_ODOO_URL= http://localhost:8077

# For Vite dev proxy: Odoo server URL (used when React runs on port 5173)
VITE_ODOO_TARGET=http://localhost:8077
